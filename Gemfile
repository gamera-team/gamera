source 'https://rubygems.org'

# Using forks from the original project, because the PRs made by Jason Rush
# to each of the gems are still pending. Once those go live, remove these
# and uncomment the corresponding entries in gamera.gemspec
gem 'sequel-fixture', git: 'https://github.com/gamera-team/sequel-fixture.git'
gem 'symbolmatrix', git: 'https://github.com/gamera-team/symbolmatrix.git'
gemspec
